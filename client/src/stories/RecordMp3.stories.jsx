import { RecordMp3 } from '../components';

export default {
    title: 'RecordMp3',
    component: RecordMp3,
};

const Template = (args) => <RecordMp3 {...args} />;

export const Primary = Template.bind({});
