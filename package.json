{
    "name": "test-site",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "client": "npm start --prefix client",
        "server": "npm start --prefix server",
        "build": "npm run env-dist && npm run build --prefix client",
        "test": "npm run build && npm run server",
        "install-all": "npm i && npm i --prefix client && npm i --prefix server",
        "env-dist": "cp .env ./server/.env && cp .env ./client/.env"
    },
    "author": "C4 Patino",
    "license": "ISC",
    "dependencies": {
        "build": "^0.1.4",
        "create-react-app": "^5.0.0",
        "dotenv": "^16.0.0",
        "express-generator": "^4.2.0",
        "js-cookie": "^3.0.1"
    },
    "devDependencies": {
        "sequelize-cli": "^6.4.1"
    }
}
